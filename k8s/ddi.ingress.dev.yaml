apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: omicsdi-webservice-dev
  namespace: dev
spec:
  rules:
    - host: dev.omicsdi.org
      http:
        paths:
          - path: /profilews
            backend:
              serviceName: omicsdi-ddi-webservice
              servicePort: 8080
